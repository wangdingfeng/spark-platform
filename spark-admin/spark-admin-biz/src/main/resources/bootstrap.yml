server:
  port: 9020
spring:
  application:
    name: spark-admin
  profiles:
    active: @profiles.active@
  cloud:
    nacos:
      config:
        server-addr: @nacos-url@
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        server-addr: @nacos-url@